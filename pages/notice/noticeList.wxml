<navigator wx:for="{{noticeList}}" wx:key="id" url="/pages/notice/showNotice?id={{index}}">
  <view class='cashlogs'>
    <view class='profile'>
      <view class='typeStr'>{{item.notice_title}}</view>
      <view class='dateAdd'>{{ item.notice_time}}</view>
    </view>
      <view class='amount'> 查看 > </view>
  </view>
</navigator>